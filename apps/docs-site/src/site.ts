// Live Mintlify documentation site.
// Placeholder default — replace with your real Mintlify URL after connecting
// the repo in the Mintlify dashboard (e.g. https://aspidasec.mintlify.app or a
// custom domain). Every "docs" link on the marketing site reads from here.
export const DOCS_URL = 'https://aspidasec.mintlify.app'

export const docs = (path = '') => `${DOCS_URL}${path}`
