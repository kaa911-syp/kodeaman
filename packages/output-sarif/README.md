# @aspidasec/output-sarif

SARIF output formatter for AspidaSec (placeholder).

## Responsibility

- Convert NormalizedFinding arrays into SARIF v2.1.0 JSON format.
- Enable integration with GitHub Code Scanning, VS Code SARIF Viewer, and other SARIF-compatible tools.

## Status

This package is a **placeholder** — `src/` is empty and no `package.json` exists yet. See `ROADMAP.md` for the planned output format milestones.

## Source of truth

- SARIF generator will live in `src/` once implemented.
- Output schema contracts are defined in `@aspidasec/schema`.
