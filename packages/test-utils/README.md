# @aspidasec/test-utils

Shared test utilities and fixtures for AspidaSec packages (placeholder).

## Responsibility

- Provide reusable test helpers, mock data factories, and fixture builders.
- Keep test boilerplate consistent across workspace packages.

## Status

This package is a **placeholder** — `src/` is empty and no `package.json` exists yet.

## Source of truth

- Test helpers will live in `src/` once implemented.
- Fixture data conventions follow `@aspidasec/schema` type definitions.
